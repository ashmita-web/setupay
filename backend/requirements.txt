fastapi==0.104.1
uvicorn[standard]==0.24.0
sqlalchemy==2.0.23
psycopg2-binary==2.9.9
pydantic[email]==2.5.2
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
PyNaCl==1.5.0
scikit-learn==1.3.2
joblib==1.3.2
numpy==1.26.2
pandas==2.1.4
python-multipart==0.0.6
# Pinned below 4.1: passlib 1.7.4 probes bcrypt.__about__.__version__, which
# 4.1 removed. passlib traps the AttributeError and logins still succeed, but
# it prints a full traceback on every hash — alarming on a projected terminal.
bcrypt==4.0.1
httpx==0.27.0
cryptography>=3.3.2
# The Gemini provider (the default) needs nothing beyond httpx, already above.
# The Anthropic provider is optional and imported lazily, so it is left out to
# keep the Render free-tier build small and fast. Install it only if you set
# EXPLAINER_PROVIDER=anthropic:
#   anthropic>=0.40.0
